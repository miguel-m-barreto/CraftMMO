# Dependencies

The root POM is the version authority for dependency and plugin versions.

Allowed foundation dependencies include Paper API as provided, Flyway, HikariCP, PostgreSQL JDBC, JUnit 5, Testcontainers, Jackson for reference manifest parsing, and Maven verification plugins.

Do not add Lombok, Spring, Hibernate, JPA, Kotlin, Gradle, or mcMMO dependencies.
